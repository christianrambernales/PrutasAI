{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run compile:knowledge && npm run sync:models && npm --prefix app run build:web",
  "outputDirectory": "app/dist",
  "installCommand": "npm install && npm --prefix app install && npm --prefix api install",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
        { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" }
      ]
    }
  ]
}
